interface:
  display_name: Metatron Majordomo
  short_description: Send simple bounded work to a local model
  default_prompt: Use majordomo for simple, bounded work when the configured backend is available.
